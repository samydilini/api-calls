// Load environment variables
require("dotenv").config({ path: ".env.local" });

// Optionally run migrations or other setup code
// const { execSync } = require("child_process");
// execSync("npx prisma migrate deploy");
